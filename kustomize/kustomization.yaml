apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./default

# Fleet deploys through Helm; align this label to avoid persistent Modified drift.
labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/managed-by: Helm
